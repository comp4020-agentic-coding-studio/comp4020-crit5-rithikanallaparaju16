node_modules/
dist/
*.log

# editor and OS junk
.DS_Store
._*
Thumbs.db
Desktop.ini

# keep secrets out of the repo: your API key lives in an env var or an
# untracked file, never in a tracked one
.env
.env.*
.envrc
mise.local.toml
*.pem

# Claude Code's project directory is machine-local: settings in here can hold
# your course API key, and this repo goes public when you ship. Your tracked
# harness is CLAUDE.md (and any AGENTS.md), not this directory.
.claude/

# Design exports: 5MB of Stitch mockups, kept locally as reference but not
# shipped. Also named in vite.config.ts SKIP --- ignoring alone would still
# let the build turn each code.html into a deployed page.
stitch_lunar_gravity_racer/
